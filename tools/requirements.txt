# As ferramentas de Python do projeto, fixadas. O CI instala daqui e o
# `make ferramentas-python` tambem — para que a versao que chumba na tua
# maquina seja a mesma que chumba no runner.
#
# O dependabot vigia este ficheiro (.github/dependabot.yml, directory: /tools).
gdtoolkit==4.5.0
